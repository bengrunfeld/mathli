import { InstructionsBox, InstructionsText } from "./Instructions.styles";

const Instructions = () => (
  <InstructionsBox>
    <InstructionsText>
      Type your answer and hit enter key or return button
    </InstructionsText>
  </InstructionsBox>
);

export default Instructions;
